const validateBody = require("./validateBody");
const validateByUpdate = require("./validateByUpdate");
const isValidId = require("./isValidId");
module.exports = { validateBody, isValidId, validateByUpdate };
